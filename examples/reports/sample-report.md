# Agentability Audit Sample

Overall score: 86/100

This sample artifact shows the shape of a mature report:

- Scores for semantic discoverability, actionability, state feedback, recoverability, and agent safety.
- Native observation evidence from the selected backend.
- Task probe replay with state IDs, DOM delta summary, network summary, console summary, and duration.
- Prioritized issues with rule IDs and repair guidance.

Render the JSON sample:

```bash
npx agentability report examples/reports/sample-report.json --html reports/sample.html --open
```
